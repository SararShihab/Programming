#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, powr;

    cout << "Ente the base and power separated by space: ";
    cin >> a >> b;

    powr = pow(a, b);

    cout << powr << endl;

    return 0;
}